# React + Node.js Chat App

This project is a real-time chat app with backend (Node.js + Express + MongoDB + Socket.IO) and frontend (React + Socket.IO).

# Folder Structure

- `server/` → Node.js backend
- `client/` → React frontend# Chat_MERN
MERN Stack
